# Cutover DAY-1, primo tentativo — 2026-09-08

> Evidence datata, non authority. Lo stato delle righe resta in
> `docs/work/day1/requirements-status.md`; la domanda terminale in
> `docs/work/day1/readiness-criteria.md`.

Sessione a contesto fresco con un solo goal: `DAY_1_CAN_START`. Nessuna
capability nuova; ogni lavoro nato da un failure osservato installando davvero.

## Candidato e installazione

- `dev` = 4521f83 (dopo #478 D13 e #479, sotto). Zero PR aperte, zero deleghe
  aperte o parcheggiate, worktree puliti (un worktree sporco del 03/09 è stato
  preservato come `wip/handoff-ffb3a5-2026-09-03` e rimosso).
- Installazione personale dell'owner (macOS, launchd): `muffin update --channel
  dev` da fe8d55e a 2b6de29 e poi a 4521f83; backup automatico prima del flip,
  schema v6 invariato, gateway riavviato e verificato per pid.
- `muffin backup` + `muffin restore --yes` reali sull'installazione (gateway
  fermo): turni intatti, schema v6, gateway riacceso sotto launchd.

## Difetti trovati solo installando davvero

1. **Tetto tool.** Sull'installazione vera `doctor` contava 20 tool registrati
   con tetto 19: `sys_inspect` tagliato, cioè E7 (auto-ispezione) non
   raggiungeva il modello. D15 aveva alzato 18→19 da un conteggio inferiore di
   uno a quello reale. Chiuso da #479 (19→20, misurato), gate `ci:local` PASS
   in seconda corsa (la prima `DISCARDED` per contesa dell'host).
2. **`install.sh` su una VPS x86_64 vergine (Hetzner, Ubuntu 24.04, utente
   creato da root con `su -`).** Due difetti che il container di A11 non vede:
   (a) il launcher `~/.local/bin/muffin` è un symlink con shebang
   `#!/usr/bin/env node`, ma il Node che l'installer mette sotto
   `~/.local/share/muffin/node/bin` non è nel PATH di nessuna shell: su una
   macchina senza Node di sistema `muffin` non parte dopo l'installer
   (`/usr/bin/env: 'node': No such file or directory`); il test
   `evals/install/ubuntu.sh` ri-aggiunge quel PATH a mano e per questo resta
   verde. (b) Da `su - muffin` non esiste il bus systemd dell'utente («Failed
   to connect to bus: No medium found»): la unit viene scritta, il gateway non
   parte, exit 3. Sulla VPS è il caso normale, non un caso limite. Entrambi
   sono difetti meccanici dentro A11; il rimedio a mano è aggiungere
   `~/.local/share/muffin/node/bin` e `~/.local/bin` al PATH e, da root,
   `loginctl enable-linger <utente>` prima di `muffin gateway install
   --write --start`.
3. **Sudo.** L'utente appena creato non ha sudo senza password: bubblewrap,
   socat e ripgrep non vengono installati, la sandbox è spenta e ogni comando
   shell chiede conferma. Dichiarato dallo script, non silenzioso.

## La prova memoria, cross-session, sull'installazione vera

Richiesta dall'owner perché `docs/VISION.md` (07/09) considera remember /
correct / forget intenti conversazionali normali mentre `docs/ROADMAP.md`
dichiara non implementato il proposal path intenzionale (ADR-0051).

Sequenza remember → recall → correct → recall → forget → recall con un fatto di
prova innocuo, prima in sei sessioni headless (`muffin run`), poi in REPL viva
(tmux, consolidamento reale), ciascun passo in una sessione separata.

| passo | headless | REPL viva |
|---|---|---|
| remember → recall | OK, ma solo dagli episodi grezzi: la sessione headless muore prima del debounce di 20 s e il gateway non drena gli episodi di altri processi (backlog «da estrarre» cresce) | OK; il consolidatore della REPL estrae il fatto (`claims`) |
| correct → recall | OK (nota nel vault via `vault_save` + episodi; nessun fatto superato) | OK nella credenza: fatto vecchio `expired`, `superseded_by` il nuovo. Superficie: il modello tenta di riscrivere la nota del vault con `shell_run_write`, ASK a taint 3, 430 s, $0.28, chiude «non è fatto» |
| forget → recall | **FALLITO**: 448 s, 10 chiamate, `shell_run` in timeout, ricetta `rm` + `DELETE FROM chunks` consegnata all'owner; la sessione dopo risponde ancora il valore | **FALLITO**: ~6 min di ricognizione (sqlite, `ls`, lettura del codice del repository dalla cartella di lavoro) interrotta dall'owner per costo; nessun tool né comando di ritiro esiste |

Osservazioni dell'owner durante la prova: il blocco di recall porta in ogni
turno anche i valori superati e il modello li racconta («la memoria ha sempre lo
stato completo, non solo quello attivo»); la prova stessa contamina la memoria
reale. Costo: 11 turni CLI, $1.83. Dopo la prova il database è stato riportato
al backup pre-prova con `muffin restore --yes` (nessun turno dell'owner
nell'intervallo) e le note di prova rimosse dal vault.

## ZDR

Decisione owner (oggi): **Zero Data Retention del provider remoto è requisito
per i 14 giorni.** Osservato su OpenRouter (`/api/v1/endpoints/zdr`, pubblico):
`routing.only: ["alibaba"]` non è ZDR; `qwen/qwen3.8-27b` ha nove endpoint ZDR
(Phala, Novita, Parasail, CoreWeave, Venice, Reka, Io Net, AkashML, Ionstream;
quasi tutti fp8, Phala e Novita a quantizzazione non dichiarata);
`qwen/qwen3.7-flash` — il modello light di reranker, giudice della memoria ed
estrazione — ha **zero** endpoint ZDR. Il codice manda `data_collection` e mai
`provider.zdr`. Nessun cambio fatto: la scelta di modello e provider è
dell'owner.

## Il verdetto del reviewer indipendente

Contesto fresco, sola lettura, 45 tool call, HEAD 4521f83.
`DAY_1_CAN_START: NO`, due blocker, entrambi decisioni owner/product:

- **F1** nessun meccanismo di «dimentica» contro una claim VISION esplicita:
  o VISION/requisiti lo dichiarano OUT per i 14 giorni con ragione, o si
  costruisce il percorso minimo di ritiro governato, simmetrico al supersede
  già esistente per correct.
- **F2** la configurazione di produzione viola il requisito ZDR appena posto.
- **F3** (meccanico, dentro A11) il PATH di Node non persistito da
  `install.sh`: confermato sulla VPS reale, sopra.

Non bloccanti: la corsia Telegram reale non è stata rieseguita a 4521f83 (le
righe B2/B11/B13 restano provate sul filo del 04-06/09); i warning di `doctor`
sull'installazione (legame owner fuori dal sigillo, due file RoT divergenti,
arretrato vettoriale, quattro turni CLI di agosto senza indirizzo) sono
classificati e con rimedio esistente, ma il rimedio parla di «reseal» e
«harden» a un owner che non deve conoscere quelle parole (VISION, terzo
secchio: l'attrito è voluto, il lessico no).
