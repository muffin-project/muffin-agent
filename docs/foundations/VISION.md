# VISION moved

The current product north star is:

`docs/VISION.md`

This path is kept as a fail-safe for older references. Do not treat the former
foundation version as current product state.

Historical copies are preserved under `docs/history/`.